import { createTheme } from '@mui/material/styles';

const Theme = createTheme({
  palette: {
    primary: {
      main: '#84c887',
      contrastText: '#fff',
    },
    secondary: {
      main: '#d81b60',
      contrastText: '#fff',
    },
    
  },
});

export default Theme;